services:
  iobroker-rr2mqtt:
    build:
      context: .
      target: production
    volumes:
      - "./data:/app/data"
    environment:
      LOCAL_MQTT: "mqtt://mqtt-server:1883"
      RR_USERNAME: "email"
      RR_PASSWORD: "password"
